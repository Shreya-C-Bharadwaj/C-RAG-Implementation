[
  {
    "content": "    while (temp > 0) {\n        int digit = temp % 10;\n        sum += pow(digit, K);\n        temp /= 10;\n    }",
    "source": "data/codebase\\armstrong.c",
    "start_line": 4,
    "signature": "bool isArmstrong(int N) { int temp = N; int sum = 0;  // Get the number of digits // Adding 1 to compensate for the loss of fraction part // of the value returned by log10 due to the conversion // into integer int K = log10(temp) + 1;  // Calculate the sum of the digits raised to the power of // num_digits"
  },
  {
    "content": "int main() {\n    int N = 153;\n\n    // Check if the number is an Armstrong number\n    if (isArmstrong(N)) {\n        printf(\"Yes, %d is an Armstrong Number\\n\", N);\n    }\n    else {\n        printf(\"No, %d is not an Armstrong Number\\n\", N);\n    }\n\n    return 0;\n}",
    "source": "data/codebase\\armstrong.c",
    "start_line": 23,
    "signature": "return (sum == N); } "
  }
]